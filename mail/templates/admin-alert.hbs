<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <title>{{title}}</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f7fb;
      margin: 0;
      padding: 0;
    }
    .container {
      background-color: #ffffff;
      width: 100%;
      max-width: 600px;
      margin: auto;
      padding: 20px;
      border-radius: 12px;
      border-top: 6px solid #0D784A;
      box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
    }
    h2 {
      color: #0D784A;
      text-align: center;
      font-size: 24px;
      margin-bottom: 20px;
    }
    p {
      color: #333;
      font-size: 15px;
      line-height: 1.6;
    }
    a.button {
      display: inline-block;
      background-color: #0D784A;
      color: #fff;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 6px;
      font-weight: bold;
      text-align: center;
      margin-top: 20px;
    }
    a.button:hover {
      background-color: #095a36;
    }
    .footer {
      font-size: 12px;
      color: #999;
      text-align: center;
      margin-top: 20px;
      border-top: 1px solid #ddd;
      padding-top: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>{{title}}</h2>
    <p>{{message}}</p>
    {{#if reservationUrl}}
      <p><a href="{{reservationUrl}}" class="button">Ver en panel</a></p>
    {{/if}}
    <div class="footer">
      <p>© {{year}} MUDECOOP R.L.</p>
    </div>
  </div>
</body>
</html>
